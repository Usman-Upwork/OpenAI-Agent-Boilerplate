# OpenAI Configuration
OPENAI_API_KEY=your_openai_api_key_here

# Database Configuration (Local PostgreSQL)
POSTGRES_DB=chainlit_db
POSTGRES_USER=chainlit_user
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# Constructed Database URLs (don't modify these)
DB_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# Chainlit Configuration
DEFAULT_HISTORY_MODE=local_text
CHAINLIT_AUTH_SECRET=your_super_secret_jwt_key_change_this_in_production

# Vector Store Configuration
VECTOR_STORE_ID=default_store

# MCP Server Configuration (optional)
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=INFO